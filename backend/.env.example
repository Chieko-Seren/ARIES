# Vault配置
VAULT_URL=http://vault:8200
VAULT_TOKEN=your-vault-token
VAULT_MOUNT_POINT=aries

# 基本配置
DEBUG=false
LOG_DIR=./logs

# API配置
API_PREFIX=/api
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 数据库配置
DB_PATH=./data/aries.db

# LLM配置
LLM_PROVIDER=openai
LLM_MODEL=gpt-4

# 知识库配置
VECTOR_DB_PATH=./data/vector_store
KG_PATH=./data/knowledge_graph

# 服务器配置
SERVERS_CONFIG_PATH=./config/servers.json

# Kubernetes配置
KUBE_CONFIG_PATH=~/.kube/config

# 通知配置
WEBHOOK_URL=

# 搜索API配置
SEARCH_API_KEY=

# MQTT配置
MQTT_BROKER=mqtt
MQTT_PORT=1883

# CORS配置
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000 